# Local-only Claude Code settings (machine-specific permission allowlist)
.claude/settings.local.json
supabase/.temp/

# Word/Excel lock files -- created transiently whenever a .docx/.xlsx is open, never real content
~$*

# Cloudflare Workers local dev artifacts
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Internal working documents that live in the repo root but are not part of the
# site. Kept out of git so a clean-clone build (Workers Builds) never serves them.
domain-checklist.html
kerph-plan-comparison.html
overnight-report-*.html
shop-jigs-script.html
