# Local-only Claude Code settings (machine-specific permission allowlist)
.claude/settings.local.json
supabase/.temp/

# Word/Excel lock files -- created transiently whenever a .docx/.xlsx is open, never real content
~$*

# Cloudflare Workers local dev artifacts
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
